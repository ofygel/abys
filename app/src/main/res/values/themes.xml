<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- фиксированная тёмная тема без ActionBar -->
    <style name="Theme.Abys" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>

        <!-- делаем статус-бар прозрачным, нав-бар чёрным -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <!-- Splash: тот же набор + полноэкран -->
    <style name="Theme.Abys.NoActionBar" parent="Theme.Abys">
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
